# 多智能体系统依赖安装指南

## 问题说明

如果遇到以下错误：
```
ModuleNotFoundError: No module named 'langchain_community'
```

这是因为缺少必要的Python依赖包。

## 解决方案

### 方式1: 安装所有依赖（推荐）

```bash
# 安装后端依赖
pip install -r backend/requirements.txt

# 或安装根目录依赖
pip install -r requirements.txt
```

### 方式2: 单独安装缺失的包

```bash
pip install langchain-community
pip install langchain-core
pip install langgraph
pip install langchain
pip install dashscope
pip install chromadb
pip install fastapi
pip install uvicorn
```

### 方式3: 使用虚拟环境（推荐用于开发）

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r backend/requirements.txt
```

## 验证安装

安装完成后，运行以下命令验证：

```bash
# 验证导入
python -c "from agent.multi_agent_system import MultiAgentTrainingSystem; print('✓ 导入成功')"

# 运行测试
python test_multi_agent.py

# 运行演示
python start_multi_agent_demo.py
```

## 常见问题

### Q1: pip安装速度慢
使用国内镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: 权限错误
使用用户安装：
```bash
pip install --user -r requirements.txt
```

### Q3: 版本冲突
升级pip并重新安装：
```bash
python -m pip install --upgrade pip
pip install -r requirements.txt --upgrade
```

## 最小依赖列表

如果只想运行多智能体系统，最少需要：

```
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langgraph>=0.0.20
dashscope>=1.14.0
chromadb>=0.4.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
```

## 环境要求

- Python 3.8 或更高版本
- pip 最新版本
- 足够的磁盘空间（约500MB用于依赖）

## 配置API密钥

安装依赖后，还需要配置API密钥：

1. 复制 `model/.env.example` 为 `model/.env`（如果存在）
2. 或创建 `model/.env` 文件
3. 添加以下内容：

```env
DASHSCOPE_API_KEY=your_api_key_here
```

获取API密钥：https://dashscope.console.aliyun.com/

## 完整启动流程

```bash
# 1. 安装依赖
pip install -r backend/requirements.txt

# 2. 配置API密钥
# 编辑 model/.env 文件

# 3. 测试多智能体系统
python test_multi_agent.py

# 4. 启动后端服务
cd backend
python api.py

# 5. 启动前端（新终端）
cd frontend
npm install
npm run dev
```

## 故障排查

如果仍然遇到问题：

1. 检查Python版本：`python --version`（需要3.8+）
2. 检查pip版本：`pip --version`
3. 清除pip缓存：`pip cache purge`
4. 重新安装：`pip install -r requirements.txt --force-reinstall`
5. 查看详细错误：`python -v -c "from agent.multi_agent_system import MultiAgentTrainingSystem"`

## 联系支持

如果问题仍未解决，请：
1. 查看日志文件：`logs/agent_YYYYMMDD.log`
2. 检查系统环境
3. 提供完整的错误信息
